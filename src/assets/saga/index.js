/*
import {all} from 'redux-saga/effects'
import {pathWatcher} from "./PathSaga";
import {currentPathWatcher} from "./CurrentPathSaga";

export function* rootWatcher() {
    yield all([pathWatcher(), currentPathWatcher()]);
}
*/